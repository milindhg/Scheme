#lang racket
(require C311/trace)
(require C311/pmatch)

(define last-non-zero
  (lambda (ls) 
    (call/cc
     (lambda (k)
       (letrec
         ((lnz
           (lambda (ls)
 
 
 
	     )))
         (lnz ls))))))