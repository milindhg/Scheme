#lang racket
(require C311/trace)

(define times-cps
  (lambda (ls k)
    (cond
      [(null? ls) (k 1)]
      [(zero? (car ls)) (k 0)]
      [else (times-cps (cdr ls) (lambda (x) (k (* (car ls) x))))])))

(times-cps '(1 2 3 4 5) (lambda (x) x))
;120
(times-cps '(1 2 3 0 3) (lambda (x) x))
;0
