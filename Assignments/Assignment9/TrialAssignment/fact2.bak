#lang racket

;;Factorial trial based on the parentheC/PC tutorial
;File 1

;Standard recursive definition of factorial

(define fact_cps
  (lambda (n k)
    (cond
      [(zero? n) (k 1)]
      [else (fact (sub1 n) (lambda (x) (* n x)))])))

(define main
  (lambda ()
    (fact 5)))

(main)

